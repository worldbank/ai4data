# Default Agents Manifest

The bundled default manifest defines the standard five-agent pipeline. The source file lives in the package:

```{literalinclude} ../../../src/ai4data/metadata/reviewer/agents_manifest/default_agents_manifest.yml
:language: yaml
```
